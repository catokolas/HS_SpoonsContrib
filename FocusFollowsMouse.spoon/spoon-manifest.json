{
  "schemaVersion": 1,
  "name": "FocusFollowsMouse",
  "version": "0.1",
  "description": "Sloppy focus / X11-style focus-follows-mouse: when the pointer comes to rest over a window that isn't focused, that window is focused.",
  "author": "Cato Kolås <cato.kolas@gmail.com>",
  "homepage": "https://github.com/catokolas/HS_SpoonsContrib",
  "license": "MIT",
  "lifecycle": {
    "hasStart": true,
    "hasStop": true,
    "hasToggle": true,
    "hasConfigure": true,
    "eventDriven": false
  },
  "config": [
    {
      "key": "delay",
      "label": "Focus delay",
      "description": "Seconds the pointer must rest over a window before focus is changed.",
      "type": "number",
      "default": 0.1,
      "min": 0.0,
      "max": 2.0,
      "step": 0.01,
      "unit": "s"
    },
    {
      "key": "excludedApps",
      "label": "Excluded apps",
      "description": "App names or bundle IDs whose windows should never be auto-focused.",
      "type": "stringList",
      "default": [],
      "itemPlaceholder": "e.g. Safari, Notification Center, org.keepassxc.keepassxc"
    }
  ],
  "hotkeys": [
    {
      "action": "toggle",
      "label": "Toggle on/off",
      "default": { "mods": ["shift", "ctrl", "cmd"], "key": "f" }
    }
  ],
  "optionalModules": [
    {
      "name":          "hs._ckol.sloppyfocus",
      "repo":          "catokolas/HS_ModulesContrib-sloppyfocus",
      "installSubdir": "hs/_ckol/sloppyfocus",
      "assetPattern":  "sloppyfocus-*-macos-universal.zip",
      "description":   "Focus-without-raise via macOS SkyLight private APIs. Works on Chromium-based PWAs where pure hs.window:focus() raises the window. Without it the Spoon falls back to win:focus()."
    }
  ]
}
